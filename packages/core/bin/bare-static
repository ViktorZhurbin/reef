#!/bin/bash

# Use bun if available, otherwise fall back to node
if command -v bun &> /dev/null; then
  RUNTIME="bun"
else
  RUNTIME="node"
fi

if [ "$1" = "build" ]; then
  exec $RUNTIME "$(dirname "$0")/../lib/build.js" "$@"
else
  exec $RUNTIME "$(dirname "$0")/../lib/dev.js" "$@"
fi