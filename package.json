{
	"name": "bare-static-monorepo",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter website dev",
		"build": "pnpm --filter website build",
		"format": "biome format --write",
		"loc": "find packages/core/lib -type f | xargs wc -l"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11"
	}
}
