{
	"name": "reef-monorepo",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter website dev",
		"build": "pnpm --filter website build",
		"format": "biome format --write",
		"loc": "find -E packages/reef/{lib,islands,utils} -type f -regex '.*.(js|ts)$' | xargs wc -l"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11"
	}
}
